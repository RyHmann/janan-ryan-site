import { useEffect, useState } from 'react';
export default function AdminCallback() { const [message, setMessage] = useState('Signing you in…'); useEffect(() => { const params = new URLSearchParams(location.hash.slice(1)); const accessToken = params.get('access_token'); if (!accessToken) { setMessage('This sign-in link is invalid or has expired.'); return; } fetch('/api/admin/session', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ accessToken }) }).then(async (response) => { if (!response.ok) throw new Error((await response.json()).error); location.replace('/admin'); }).catch((error) => setMessage(error instanceof Error ? error.message : 'Unable to sign you in.')); }, []); return <section className="admin-login panel"><h1>{message}</h1></section>; }
