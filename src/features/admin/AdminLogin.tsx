import { useState } from 'react';
export default function AdminLogin() { const [message, setMessage] = useState<string | null>(null); const [busy, setBusy] = useState(false); return <section className="admin-login panel"><p>Private dashboard</p><h1>Sign in to manage RSVPs</h1><form onSubmit={async (event) => { event.preventDefault(); setBusy(true); setMessage(null); const email = new FormData(event.currentTarget).get('email'); try { const response = await fetch('/api/admin/login', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ email }) }); const data = await response.json(); if (!response.ok) throw new Error(data.error); setMessage('Check your inbox for your secure sign-in link.'); } catch (error) { setMessage(error instanceof Error ? error.message : 'Unable to send sign-in link.'); } finally { setBusy(false); } }}><label>Email address<input required type="email" name="email" autoComplete="email" /></label><button disabled={busy}>{busy ? 'Sending…' : 'Email me a sign-in link'}</button></form>{message && <p role="status">{message}</p>}<style>{`.admin-login{max-width:34rem;margin:3rem auto;padding:2rem}.admin-login form{display:grid;gap:1rem}.admin-login label{display:grid;gap:.4rem}.admin-login input{padding:.7rem;border:1px solid var(--border-content);border-radius:8px}.admin-login button{justify-self:start;border:0;border-radius:999px;padding:.7rem 1rem;background:var(--action-primary);color:var(--text-on-accent);font:inherit;cursor:pointer}`}</style></section>; }
